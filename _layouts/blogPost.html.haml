---
layout: blogBase
---
#post-navigation
  .post-navigation
    #post-nav-previous
      .previous
        - if page.previous_post
          .label
            previous post
          .nav-link
            %a{:href => page.previous_post.url}= page.previous_post.title
    #post-nav-next
      .next
        - if page.next_post
          .label
            next post
          .nav-link
            %a{:href => page.next_post.url}= page.next_post.title
.post
  %h1.title 
    #{page.title}
  .byline
    .author
      by
      = partial('userBadge.html.haml', :userId => page.author)
      // there is only one author per page but the author splitter adds an array here
      // %a{:href=>page.author[0].primary_page.url} #{page.author[0]}
    .date
      on #{page.date.strftime('%d-%^b-%Y')}
  %hr
  .body
    ~ content
  .tags
    %span.glyphicon.glyphicon-tags
    &nbsp;tagged as
    - page.tags.each do |tag|
      %a{:href=>tag.primary_page.url}
        = tag
  %hr
  .comments
    = page.disqus_comments
